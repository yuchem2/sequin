services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sequin
      POSTGRES_PASSWORD: sequin_dev
      POSTGRES_DB: sequin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: